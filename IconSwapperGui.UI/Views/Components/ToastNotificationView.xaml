<UserControl x:Class="IconSwapperGui.UI.Views.Components.ToastNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:IconSwapperGui.Core.Models;assembly=IconSwapperGui.Core"
             mc:Ignorable="d"
             d:DesignWidth="400"
             d:DesignHeight="100">

    <UserControl.Resources>
        <Storyboard x:Key="SlideInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="450"
                             To="0"
                             Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0"
                             To="1"
                             Duration="0:0:0.3" />
        </Storyboard>

        <Storyboard x:Key="SlideOutAnimation">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                             From="0"
                             To="450"
                             Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1"
                             To="0"
                             Duration="0:0:0.2" />
        </Storyboard>
    </UserControl.Resources>

    <Border x:Name="ToastBorder"
            Background="{DynamicResource SurfaceBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="16"
            Width="380"
            MaxHeight="120"
            RenderTransformOrigin="0.5,0.5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24"
                              ShadowDepth="4"
                              Opacity="0.25"
                              Color="#000000" />
        </Border.Effect>
        <Border.RenderTransform>
            <TranslateTransform X="0" />
        </Border.RenderTransform>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <Border Grid.Column="0"
                    Width="40"
                    Height="40"
                    CornerRadius="8"
                    Margin="0,0,12,0"
                    VerticalAlignment="Top">
                <Border.Style>
                    <Style TargetType="Border">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Info}">
                                <Setter Property="Background" Value="#E0F2FE" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Success}">
                                <Setter Property="Background" Value="#DCFCE7" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Warning}">
                                <Setter Property="Background" Value="#FEF3C7" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Error}">
                                <Setter Property="Background" Value="#FEE2E2" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Path Width="20"
                      Height="20"
                      Stretch="Uniform"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
                    <Path.Style>
                        <Style TargetType="Path">
                            <Style.Triggers>
                                <!-- Info Icon -->
                                <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Info}">
                                    <Setter Property="Data" Value="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                                    <Setter Property="Stroke" Value="#0EA5E9" />
                                    <Setter Property="StrokeThickness" Value="2" />
                                    <Setter Property="Fill" Value="Transparent" />
                                </DataTrigger>
                                <!-- Success Icon -->
                                <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Success}">
                                    <Setter Property="Data" Value="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                    <Setter Property="Stroke" Value="#22C55E" />
                                    <Setter Property="StrokeThickness" Value="2" />
                                    <Setter Property="Fill" Value="Transparent" />
                                </DataTrigger>
                                <!-- Warning Icon -->
                                <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Warning}">
                                    <Setter Property="Data" Value="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
                                    <Setter Property="Stroke" Value="#EAB308" />
                                    <Setter Property="StrokeThickness" Value="2" />
                                    <Setter Property="Fill" Value="Transparent" />
                                </DataTrigger>
                                <!-- Error Icon -->
                                <DataTrigger Binding="{Binding Type}" Value="{x:Static models:NotificationType.Error}">
                                    <Setter Property="Data" Value="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                    <Setter Property="Stroke" Value="#EF4444" />
                                    <Setter Property="StrokeThickness" Value="2" />
                                    <Setter Property="Fill" Value="Transparent" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Path.Style>
                </Path>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding Title}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           TextWrapping="Wrap" />
                <TextBlock Text="{Binding Message}"
                           FontSize="13"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0"
                           MaxHeight="40"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="2"
                    Width="28"
                    Height="28"
                    VerticalAlignment="Top"
                    Margin="8,0,0,0"
                    Command="{Binding DismissCommand}"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="4">
                            <Path Data="M6 18L18 6M6 6l12 12"
                                  Stroke="{DynamicResource TertiaryTextBrush}"
                                  StrokeThickness="2"
                                  Width="16"
                                  Height="16"
                                  Stretch="Uniform"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
