<Window x:Class="IconSwapperGui.UI.Windows.IconVersionManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IconSwapperGui.UI.Windows"
        xmlns:converters="clr-namespace:IconSwapperGui.UI.Converters"
        mc:Ignorable="d"
        Title="Icon Version Manager"
        Height="720"
        Width="1040"
        MinHeight="600"
        MinWidth="920"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">

    <Window.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/SharedStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource IconVersionManagerCardSurfaceStyle}" Padding="18" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Width="24" Height="24" CornerRadius="12" Grid.Column="0">
                    <Path
                        Data="M6 4h10a1 1 0 011 1v10a1 1 0 01-1 1H6a1 1 0 01-1-1V5a1 1 0 011-1zm-2 4h10a1 1 0 011 1v10a1 1 0 01-1 1H4a1 1 0 01-1-1V9a1 1 0 011-1z"
                        Width="24"
                        Height="24"
                        Fill="{DynamicResource SecondaryTextBrush}"
                        Stretch="Uniform" />
                </Border>

                <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource IconVersionManagerHeaderTitleStyle}" Text="Icon Version Manager" />
                    <TextBlock Style="{StaticResource IconVersionManagerHeaderSubtitleStyle}"
                               Text="Review, preview and restore previous icon versions" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: list -->
            <Border Grid.Column="0" Style="{StaticResource IconVersionManagerCardSurfaceStyle}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource SectionTitleStyle}" Text="Versions" VerticalAlignment="Center" />
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,12,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" Height="60">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <TextBox x:Name="VersionSearchTextBox"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                         Height="60"
                                         Padding="10,0,16,0"
                                         ToolTip="Search" />

                                <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                                    <Viewbox Width="18" Height="18" VerticalAlignment="Center"
                                             HorizontalAlignment="Center">
                                        <Path
                                            Data="M10.5 3a7.5 7.5 0 105.3 12.7l4 4 1.4-1.4-4-4A7.5 7.5 0 0010.5 3zm0 2a5.5 5.5 0 110 11 5.5 5.5 0 010-11z"
                                            Fill="{DynamicResource PrimaryTextBrush}"
                                            Stretch="Uniform"
                                            Width="18"
                                            Height="18" />
                                        <Viewbox.Style>
                                            <Style TargetType="Viewbox">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition
                                                                Binding="{Binding Text, ElementName=VersionSearchTextBox}"
                                                                Value="" />
                                                            <Condition
                                                                Binding="{Binding IsKeyboardFocusWithin, ElementName=VersionSearchTextBox}"
                                                                Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Viewbox.Style>
                                    </Viewbox>

                                    <TextBlock Text="Search icons"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               Margin="10,0,0,0"
                                               VerticalAlignment="Center"
                                               IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition
                                                                Binding="{Binding Text, ElementName=VersionSearchTextBox}"
                                                                Value="" />
                                                            <Condition
                                                                Binding="{Binding IsKeyboardFocusWithin, ElementName=VersionSearchTextBox}"
                                                                Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Grid>

                    <Grid Grid.Row="2">
                        <ListBox ItemsSource="{Binding Versions}"
                                 SelectedItem="{Binding SelectedVersion}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ItemContainerStyle="{StaticResource IconVersionManagerVersionListItemStyle}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <Border Width="52" Height="52" CornerRadius="12"
                                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                                Background="{DynamicResource AppBackgroundBrush}">
                                            <Image Source="{Binding ThumbnailPath}" Stretch="Uniform"
                                                   RenderOptions.BitmapScalingMode="HighQuality" />
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="12,0,12,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Timestamp}" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"
                                                       FontSize="13" />
                                            <TextBlock Text="{Binding RelativeTimeDisplay}"
                                                       Style="{StaticResource MutedTextStyle}" Margin="0,3,0,0" />
                                            <TextBlock Text="{Binding Description}"
                                                       Style="{StaticResource MutedTextStyle}"
                                                       TextTrimming="CharacterEllipsis" Margin="0,3,0,0" />
                                        </StackPanel>

                                        <Border Grid.Column="2" VerticalAlignment="Top" CornerRadius="10"
                                                Padding="10,4" Background="{DynamicResource SuccessBrush}"
                                                Visibility="{Binding IsCurrent, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="CURRENT" Foreground="White" FontSize="10"
                                                       FontWeight="Bold" />
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Visibility="{Binding TotalVersions, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                            <Border Width="64" Height="64" CornerRadius="20" Background="{DynamicResource HoverBrush}" />
                            <TextBlock Text="No versions yet" FontWeight="SemiBold" Margin="0,14,0,0"
                                       HorizontalAlignment="Center" />
                            <TextBlock Text="Changes you make will appear here."
                                       Style="{StaticResource MutedTextStyle}" HorizontalAlignment="Center"
                                       Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="3" Margin="0,12,0,0" Background="{DynamicResource AppBackgroundBrush}"
                            CornerRadius="10" Padding="12">
                        <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource MutedTextStyle}" />
                    </Border>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="12" Background="Transparent" HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch" />

            <!-- Right: preview/details -->
            <Border Grid.Column="2" Style="{StaticResource IconVersionManagerCardSurfaceStyle}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource SectionTitleStyle}" Text="Preview" />
                    </Grid>

                    <Grid Grid.Row="1" Margin="0,14,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="260" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{DynamicResource AppBackgroundBrush}" CornerRadius="16"
                                Padding="18" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                            <Viewbox>
                                <Border Width="300" Height="300" CornerRadius="24"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2"
                                        Background="White">
                                    <Image Source="{Binding PreviewImagePath}" Stretch="Uniform"
                                           RenderOptions.BitmapScalingMode="HighQuality" />
                                </Border>
                            </Viewbox>
                        </Border>

                        <Border Grid.Column="1" Margin="14,0,0,0" Background="{DynamicResource AppBackgroundBrush}"
                                CornerRadius="16" Padding="16" BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Details" FontWeight="SemiBold" />

                                <Grid Margin="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Date"
                                               Style="{StaticResource MutedTextStyle}" Margin="0,0,10,8" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedVersion.Timestamp}"
                                               Margin="0,0,0,8" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Size"
                                               Style="{StaticResource MutedTextStyle}" Margin="0,0,10,8" />
                                    <TextBlock Grid.Row="1" Grid.Column="1"
                                               Text="{Binding SelectedVersion.FileSizeDisplay}" Margin="0,0,0,8" />

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Path"
                                               Style="{StaticResource MutedTextStyle}" Margin="0,0,10,0"
                                               VerticalAlignment="Top" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedVersion.IconPath}"
                                               TextWrapping="Wrap" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border Grid.Row="2" Background="{DynamicResource AppBackgroundBrush}" CornerRadius="16"
                            Padding="14" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Tip:" FontWeight="SemiBold" />
                                <TextBlock Text=" You can keep this window open while you edit icons."
                                           Style="{StaticResource MutedTextStyle}" />
                            </StackPanel>
                            <Button Grid.Column="1" Style="{StaticResource IconVersionManagerSecondaryButtonStyle}"
                                    Content="Open folder" Command="{Binding OpenFolderCommand}" Padding="12,10" />
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer actions -->
        <Border Grid.Row="2" Style="{StaticResource IconVersionManagerCardSurfaceStyle}" Padding="14" Margin="0,14,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconVersionManagerDangerButtonStyle}" Content="Clear history"
                            Command="{Binding ClearHistoryCommand}" />
                    <Button Style="{StaticResource IconVersionManagerDangerButtonStyle}" Content="Delete selected"
                            Command="{Binding DeleteSelectedVersionCommand}" Margin="10,0,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconVersionManagerPrimaryButtonStyle}" Content="Restore selected"
                            Command="{Binding RevertToSelectedVersionCommand}" />
                    <Button Style="{StaticResource IconVersionManagerSecondaryButtonStyle}" Content="Close"
                            Command="{Binding CloseCommand}" Margin="10,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="3" Background="#7F000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="{DynamicResource SurfaceBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1" CornerRadius="16" Padding="18"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Width="260">
                    <ProgressBar Height="6" IsIndeterminate="True" Foreground="{DynamicResource AccentBrush}"
                                 Background="{DynamicResource DividerBrush}" />
                    <TextBlock Text="Loading versions…" Margin="0,12,0,0" FontWeight="SemiBold" />
                    <TextBlock Text="This should only take a moment." Style="{StaticResource MutedTextStyle}"
                               Margin="0,4,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>