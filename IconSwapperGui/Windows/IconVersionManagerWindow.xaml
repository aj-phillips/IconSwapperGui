<Window x:Class="IconSwapperGui.Windows.IconVersionManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:utilities="clr-namespace:IconSwapperGui.Utilities"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Icon Version Manager"
        Height="650"
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource MaterialDesignPaper}">

    <Window.Resources>
        <!-- Version Item Style -->
        <Style x:Key="VersionListBoxItemStyle" TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHueLightBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <utilities:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter" />
        <utilities:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <utilities:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="16,16,16,8" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Grid.Column="0" 
                                         Kind="History" 
                                         Width="32" 
                                         Height="32"
                                         VerticalAlignment="Center"
                                         Margin="0,0,16,0"
                                         Foreground="{DynamicResource PrimaryHueMidBrush}"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Icon Version History" 
                               FontSize="22" 
                               FontWeight="Bold"/>
                    <TextBlock Text="{Binding FileName}" 
                               FontSize="14" 
                               Foreground="{DynamicResource MaterialDesignBodyLight}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="16,8,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Version List -->
            <materialDesign:Card Grid.Column="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- List Header -->
                    <Border Grid.Row="0" 
                            Background="{DynamicResource MaterialDesignToolBarBackground}"
                            Padding="16,12">
                        <Grid>
                            <TextBlock Text="Version History" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        Command="{Binding RefreshCommand}"
                                        ToolTip="Refresh">
                                    <materialDesign:PackIcon Kind="Refresh"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Version List -->
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding Versions}"
                             SelectedItem="{Binding SelectedVersion}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ItemContainerStyle="{StaticResource VersionListBoxItemStyle}"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="8">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Thumbnail -->
                                    <Border Grid.Column="0"
                                            Width="48"
                                            Height="48"
                                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Margin="0,0,12,0">
                                        <Image Source="{Binding ThumbnailPath}"
                                               Stretch="Uniform"/>
                                    </Border>

                                    <!-- Version Info -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TimestampDisplay}"
                                                   FontWeight="SemiBold"
                                                   FontSize="14"/>
                                        <TextBlock Text="{Binding RelativeTime}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding Description}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <!-- Current Badge -->
                                    <materialDesign:Chip Grid.Column="2"
                                                         Content="CURRENT"
                                                         Visibility="{Binding IsCurrent, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                         Background="{DynamicResource PrimaryHueMidBrush}"
                                                         Foreground="White"
                                                         FontSize="10"
                                                         FontWeight="Bold"
                                                         Padding="8,4"
                                                         VerticalAlignment="Top"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Status Bar -->
                    <Border Grid.Row="2"
                            Background="{DynamicResource MaterialDesignToolBarBackground}"
                            Padding="16,8">
                        <TextBlock Text="{Binding StatusMessage}"
                                   FontSize="12"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    </Border>

                    <!-- Empty State -->
                    <StackPanel Grid.Row="1"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding TotalVersions, Converter={StaticResource ZeroToVisibilityConverter}}">
                        <materialDesign:PackIcon Kind="History"
                                                 Width="64"
                                                 Height="64"
                                                 Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                 HorizontalAlignment="Center"/>
                        <TextBlock Text="No version history available"
                                   FontSize="16"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                   HorizontalAlignment="Center"
                                   Margin="0,16,0,0"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1"
                          Width="8"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="Transparent"
                          ShowsPreview="False"/>

            <!-- Preview Panel -->
            <materialDesign:Card Grid.Column="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Preview Header -->
                    <Border Grid.Row="0"
                            Background="{DynamicResource MaterialDesignToolBarBackground}"
                            Padding="16,12">
                        <TextBlock Text="Preview"
                                   FontSize="16"
                                   FontWeight="SemiBold"/>
                    </Border>

                    <!-- Preview Image -->
                    <Border Grid.Row="1"
                            Padding="20"
                            Background="{DynamicResource MaterialDesignBackground}">
                        <Viewbox>
                            <Border Width="256"
                                    Height="256"
                                    BorderBrush="{DynamicResource MaterialDesignDivider}"
                                    BorderThickness="2"
                                    CornerRadius="8"
                                    Background="White">
                                <Image Source="{Binding PreviewImagePath}"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>
                        </Viewbox>
                    </Border>

                    <!-- Preview Info -->
                    <StackPanel Grid.Row="2" Margin="15,0,15,15">
                        <TextBlock Text="Version Details"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"/>
                        
                        <Grid Visibility="{Binding SelectedVersion, Converter={StaticResource NullToVisibilityConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                       Text="Date:" 
                                       FontSize="12"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                                       Margin="0,0,8,4"/>
                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Text="{Binding SelectedVersion.TimestampDisplay}"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>

                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="Size:"
                                       FontSize="12"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                                       Margin="0,0,8,4"/>
                            <TextBlock Grid.Row="1" Grid.Column="1"
                                       Text="{Binding SelectedVersion.FileSizeDisplay}"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>

                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="Path:"
                                       FontSize="12"
                                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                                       Margin="0,0,8,0"
                                       VerticalAlignment="Top"/>
                            <TextBlock Grid.Row="2" Grid.Column="1"
                                       Text="{Binding SelectedVersion.IconPath}"
                                       FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Actions -->
        <materialDesign:Card Grid.Row="2" Margin="16,8,16,16" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Danger Actions -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Command="{Binding ClearHistoryCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Foreground="{DynamicResource ValidationErrorBrush}"
                            BorderBrush="{DynamicResource ValidationErrorBrush}"
                            Margin="0,0,8,0">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="DeleteForever" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock Text="CLEAR HISTORY" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Command="{Binding DeleteVersionCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Foreground="{DynamicResource ValidationErrorBrush}"
                            BorderBrush="{DynamicResource ValidationErrorBrush}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Delete" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <TextBlock Text="DELETE VERSION" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>
                </StackPanel>

                <!-- Primary Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Command="{Binding RevertCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,8,0"
                            Padding="24,8">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Restore" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="REVERT TO THIS VERSION" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Content="CLOSE"
                            Command="{Binding CloseCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Padding="24,8"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Loading Overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="3"
              Background="#AA000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True"
                             Width="60"
                             Height="60"/>
                <TextBlock Text="Loading history..."
                           Foreground="White"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>