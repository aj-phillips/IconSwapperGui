<UserControl x:Class="IconSwapperGui.UserControls.SwapperUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:utilities="clr-namespace:IconSwapperGui.Utilities"
             mc:Ignorable="d" Height="600" Width="1100">

    <UserControl.Resources>
        <utilities:IconPathToImageConverter x:Key="IconPathToImageConverter" />
        <utilities:ApplicationPathToImageConverter x:Key="ApplicationPathToImageConverter" />
        <utilities:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <utilities:InverseBooleanConverter x:Key="InverseBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="AccentTabRadioButton" TargetType="RadioButton" BasedOn="{StaticResource MaterialDesignTabRadioButton}">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="4" />
                            </Grid.RowDefinitions>
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       TextAlignment="Center" />
                            <Border Grid.Row="1" x:Name="Underline" Background="White" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}" />
                                <Setter TargetName="Underline" Property="Background" Value="{DynamicResource PrimaryHueMidBrush}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Underline" Property="Background" Value="{DynamicResource PrimaryHueMidBrush}" />
                                <Setter TargetName="Underline" Property="Opacity" Value="0.25" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Margin="15,15,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Sources -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,10,0"
                                materialDesign:ElevationAssist.Elevation="Dp3"
                                VerticalAlignment="Stretch">
                <Grid VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Tab Selector -->
                    <Grid Grid.Row="1" Margin="15,15,15,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <RadioButton Grid.Column="0" 
                                    Style="{StaticResource AccentTabRadioButton}"
                                    Margin="0,0,5,0"
                                    Content="Shortcuts"
                                    IsChecked="{Binding IsFolderTabSelected, Converter={StaticResource InverseBooleanConverter}, Mode=TwoWay}" />

                        <RadioButton Grid.Column="1" 
                                    Style="{StaticResource AccentTabRadioButton}"
                                    Margin="5,0,0,0"
                                    Content="Folders"
                                    IsChecked="{Binding IsFolderTabSelected, Mode=TwoWay}" />
                    </Grid>

                    <!-- Content Area -->
                    <Grid Grid.Row="2" Margin="15,0,15,15">
                        <!-- Shortcuts Content -->
                        <Grid Visibility="{Binding IsFolderTabSelected, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Button Grid.Row="0" Name="ChooseApplicationShortcutFolderButton"
                                   Command="{Binding ChooseApplicationShortcutFolderCommand}"
                                   Margin="0,0,0,10"
                                   HorizontalAlignment="Stretch"
                                   Height="48"
                                   Style="{StaticResource MaterialDesignRaisedDarkButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FolderPlus" 
                                                           Width="20" Height="20"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,10,0" />
                                    <TextBlock Text="Add Shortcuts Location" 
                                              VerticalAlignment="Center"
                                              FontWeight="Medium"
                                              Foreground="White" />
                                </StackPanel>
                            </Button>

                            <Border Grid.Row="1" 
                                   BorderBrush="{DynamicResource MaterialDesignDivider}"
                                   BorderThickness="1"
                                   CornerRadius="4"
                                   VerticalAlignment="Stretch">
                                <ListBox Name="ApplicationsListBox"
                                        ItemsSource="{Binding Applications}"
                                        SelectedItem="{Binding SelectedApplication, Mode=TwoWay}"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        VerticalAlignment="Stretch"
                                        Height="Auto">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="5" ToolTip="{Binding Name}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" 
                                                       Background="{DynamicResource MaterialDesignCardBackground}"
                                                       CornerRadius="4"
                                                       Padding="5"
                                                       Margin="0,0,10,0">
                                                    <Image Source="{Binding TargetPath, Converter={StaticResource IconPathToImageConverter}}"
                                                          Width="32" Height="32" />
                                                </Border>
                                                <TextBlock Grid.Column="1" 
                                                          Text="{Binding Name}"
                                                          VerticalAlignment="Center"
                                                          FontSize="14"
                                                          TextTrimming="CharacterEllipsis" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                    <ListBox.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Manage Icon Versions"
                                                     Command="{Binding ManageVersionsContextCommand}">
                                                <MenuItem.Icon>
                                                    <materialDesign:PackIcon Kind="History" />
                                                </MenuItem.Icon>
                                            </MenuItem>
                                        </ContextMenu>
                                    </ListBox.ContextMenu>
                                </ListBox>
                            </Border>

                            <Button Grid.Row="2" 
                                   Command="{Binding ManageDirectoriesCommand}"
                                   Margin="0,10,0,0"
                                   HorizontalAlignment="Stretch"
                                   Height="40"
                                   Style="{StaticResource MaterialDesignOutlinedButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FolderCog" 
                                                           Width="18" Height="18"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,8,0" />
                                    <TextBlock Text="Manage Directories" 
                                              VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </Grid>

                        <!-- Folders Content -->
                        <Grid Visibility="{Binding IsFolderTabSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Button Grid.Row="0" Name="ChooseFoldersFolderButton"
                                   Command="{Binding ChooseFoldersFolderCommand}"
                                   Margin="0,0,0,10"
                                   HorizontalAlignment="Stretch"
                                   Height="48"
                                   Style="{StaticResource MaterialDesignRaisedDarkButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FolderPlus" 
                                                           Width="20" Height="20"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,10,0" />
                                    <TextBlock Text="Add Folders Location" 
                                              VerticalAlignment="Center"
                                              FontWeight="Medium"
                                              Foreground="White" />
                                </StackPanel>
                            </Button>

                            <Border Grid.Row="1" 
                                   BorderBrush="{DynamicResource MaterialDesignDivider}"
                                   BorderThickness="1"
                                   CornerRadius="4"
                                   VerticalAlignment="Stretch">
                                <ListBox ItemsSource="{Binding Folders}"
                                        SelectedItem="{Binding SelectedFolder, Mode=TwoWay}"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        VerticalAlignment="Stretch"
                                        Height="Auto">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="5" ToolTip="{Binding Name}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <materialDesign:PackIcon Grid.Column="0" 
                                                                        Kind="Folder" 
                                                                        Width="32" Height="32" 
                                                                        Margin="0,0,10,0"
                                                                        Foreground="{DynamicResource PrimaryHueMidBrush}" />
                                                <TextBlock Grid.Column="1" 
                                                          Text="{Binding Name}" 
                                                          VerticalAlignment="Center"
                                                          FontSize="14" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>

                            <Button Grid.Row="2" 
                                   Command="{Binding ManageDirectoriesCommand}"
                                   Margin="0,10,0,0"
                                   HorizontalAlignment="Stretch"
                                   Height="40"
                                   Style="{StaticResource MaterialDesignOutlinedButton}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FolderCog" 
                                                           Width="18" Height="18"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,8,0" />
                                    <TextBlock Text="Manage Directories" 
                                              VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Grid>
                </Grid>
            </materialDesign:Card>

            <!-- Right Panel - Icons -->
            <materialDesign:Card Grid.Column="1" Margin="10,0,0,0"
                                materialDesign:ElevationAssist.Elevation="Dp3"
                                VerticalAlignment="Stretch">
                <Grid VerticalAlignment="Stretch">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Background="{DynamicResource SecondaryHueMidBrush}">
                        <TextBlock Text="Icon Library" 
                                  FontSize="16" 
                                  FontWeight="SemiBold"
                                  Margin="20,15"
                                  HorizontalAlignment="Center"
                                  Foreground="{DynamicResource MaterialDesignBody}" />
                    </Grid>

                    <!-- Search and Actions -->
                    <Grid Grid.Row="1" Margin="20,8,20,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" 
                                Name="SearchTextBox"
                                materialDesign:HintAssist.Hint="Search icons..."
                                Text="{Binding FilterString, UpdateSourceTrigger=PropertyChanged}"
                                Height="48"
                                VerticalContentAlignment="Center"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Margin="0,0,15,0"
                                FontSize="14" />

                        <Button Grid.Column="1" 
                               Name="ChooseIconFolderButton"
                               Command="{Binding ChooseIconFolderCommand}"
                               Height="48"
                               Style="{StaticResource MaterialDesignRaisedDarkButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ImagePlus" 
                                                       Width="20" Height="20"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,10,0" />
                                <TextBlock Text="Add Icons" 
                                          VerticalAlignment="Center"
                                          FontWeight="Medium"
                                          Foreground="White" />
                            </StackPanel>
                        </Button>
                    </Grid>

                    <!-- Icons Grid -->
                    <Border Grid.Row="2" 
                           BorderBrush="{DynamicResource MaterialDesignDivider}"
                           BorderThickness="1,1,1,1"
                           Margin="20,0,20,20"
                           CornerRadius="4"
                           VerticalAlignment="Stretch">
                        <ListBox Name="IconsListBox"
                                ItemsSource="{Binding FilteredIcons}"
                                SelectedItem="{Binding SelectedIcon, Mode=TwoWay}"
                                HorizontalAlignment="Stretch"
                                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                Background="Transparent"
                                BorderThickness="0"
                                VerticalAlignment="Stretch"
                                Height="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <materialDesign:Card Width="110" Height="110" 
                                                        Margin="5"
                                                        materialDesign:ElevationAssist.Elevation="Dp1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Border Grid.Row="0" 
                                                   Background="{DynamicResource MaterialDesignChipBackground}"
                                                   Padding="10">
                                                <Image Source="{Binding Path, Converter={StaticResource ApplicationPathToImageConverter}}"
                                                      Width="56" Height="56" 
                                                      Stretch="Uniform"
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Grid.Row="1" 
                                                      Text="{Binding Name}"
                                                      TextAlignment="Center"
                                                      FontSize="11"
                                                      Margin="5,8"
                                                      TextTrimming="CharacterEllipsis"
                                                      ToolTip="{Binding Name}"
                                                      MaxHeight="32"
                                                      TextWrapping="Wrap" />
                                        </Grid>
                                    </materialDesign:Card>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel HorizontalAlignment="Center" />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Open in Explorer"
                                             Command="{Binding OpenExplorerContextCommand}">
                                        <MenuItem.Icon>
                                            <materialDesign:PackIcon Kind="FolderOpen" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="Copy Path"
                                             Command="{Binding CopyPathContextCommand}">
                                        <MenuItem.Icon>
                                            <materialDesign:PackIcon Kind="ContentCopy" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem Header="Duplicate"
                                             Command="{Binding DuplicateIconContextCommand}">
                                        <MenuItem.Icon>
                                            <materialDesign:PackIcon Kind="ContentDuplicate" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem Header="Delete"
                                             Command="{Binding DeleteIconContextCommand}">
                                        <MenuItem.Icon>
                                            <materialDesign:PackIcon Kind="Delete" Foreground="#F44336" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                </ContextMenu>
                            </ListBox.ContextMenu>
                        </ListBox>
                    </Border>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Bottom Action Bar -->
        <materialDesign:Card Grid.Row="2" Margin="15,10,15,15"
                            materialDesign:ElevationAssist.Elevation="Dp2"
                            Background="{DynamicResource MaterialDesignPaper}">
            <Grid Margin="20,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding DualSwapCommand}"
                            Width="140"
                            HorizontalAlignment="Center"
                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                            Visibility="{Binding IsFolderTabSelected, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="SwapHorizontal" Grid.Column="0" VerticalAlignment="Center"
                                                     Margin="0,0,5,0" />
                            <TextBlock Text="Swap Icon" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                       Foreground="White" />
                        </Grid>
                    </Button>
                    <!--<Folder-specific swap button retained but hidden; main DualSwapCommand handles behavior-->
                    <Button Visibility="{Binding IsFolderTabSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Width="140"
                            HorizontalAlignment="Center"
                            Style="{StaticResource MaterialDesignRaisedDarkButton}"
                            IsEnabled="{Binding CanSwapFolderIcons}"
                            Command="{Binding SwapFolderIconCommand}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="SwapHorizontal" Grid.Column="0" VerticalAlignment="Center"
                                                     Margin="0,0,5,0" />
                            <TextBlock Text="Swap Icon" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                       Foreground="White" />
                        </Grid>
                    </Button>
                    <materialDesign:PackIcon Kind="Check" VerticalAlignment="Center" HorizontalAlignment="Center"
                                             Foreground="Green" Margin="10,0,0,0" Height="30" Width="30"
                                             Visibility="{Binding IsTickVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>